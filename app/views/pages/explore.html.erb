<div class="row">

	<div class = "col-xs-6">
		<% for @p in @posts %>
		<div class="panel panel-default">
			<div class="panel-body">
				<p><%= @p.content + " - " + @p.created_at.to_s %></p>
			</div>
		</div>
		<% end %>
	</div>

</div>
